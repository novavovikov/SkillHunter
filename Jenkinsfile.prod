pipeline {
  agent any

   stages {
    stage('Build') {
      steps {
        sh 'docker-compose build --no-cache'
      }
    }

    stage('Start') {
      steps {
        sh 'docker-compose up -d'
      }
    }
  }
}
