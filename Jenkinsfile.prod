pipeline {
  agent any

   stages {
    stage('Build') {
      steps {
        sh 'docker-compose build --no-cache'
        sh 'docker-compose stop'
      }
    }

    stage('Start') {
      steps {
        sh 'docker-compose up -d'
      }
    }
  }
}
