import React, { FC } from 'react'

const FatalError: FC = () => {
  return (
    <div className={'fatal-error'}>
      Something went wrong. Check server connection
    </div>
  )
}

export default FatalError
